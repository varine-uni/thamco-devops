@model List<Product>

<h2>Search Results</h2>

@if (Model != null && Model.Count > 0)
{
    <ul>
        @foreach (var product in Model)
        {
            <li>@product.Name (£@product.Price): Quantity: @product.QuantityAvailable</li>
        }
    </ul>
}
else
{
    <p>No results found.</p>
}